[package]
name = "agent-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
skald-claude = { workspace = true }
skald-codex = { workspace = true }
skald-opencode = { workspace = true }
specta = { workspace = true, optional = true }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = { workspace = true }
toml = { workspace = true }

[features]
specta = ["dep:specta"]
