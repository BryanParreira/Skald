[package]
name = "audio-chunking"
version = "0.1.0"
edition = "2024"

[features]
default = ["vad"]
vad = [
  "dep:futures-util",
  "dep:skald-audio-interface",
  "dep:skald-onnx",
  "dep:skald-vad",
  "dep:pin-project",
]
segmentation = []

[dev-dependencies]
futures-util = { workspace = true }
hound = { workspace = true }
rodio = { workspace = true }
skald-data = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

[[example]]
name = "chunking"
required-features = ["vad"]

[dependencies]
skald-audio-interface = { workspace = true, optional = true }
skald-onnx = { workspace = true, optional = true }
skald-vad = { workspace = true, features = ["silero-onnx"], optional = true }

futures-util = { workspace = true, optional = true }
pin-project = { workspace = true, optional = true }
thiserror = { workspace = true }
