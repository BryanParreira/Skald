[package]
name = "local-stt-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
specta = ["dep:specta"]

[dependencies]
serde = { workspace = true }
skald-local-model = { workspace = true }
specta = { workspace = true, optional = true }
