[package]
name = "local-stt-server"
version = "0.1.0"
edition = "2024"

[features]
default = []
specta = ["dep:specta"]
tauri-event = ["specta", "dep:tauri-specta"]
whisper-cpp = ["dep:notiz-transcribe-whisper-local"]
coreml = ["whisper-cpp", "notiz-transcribe-whisper-local/coreml"]
directml = ["whisper-cpp", "notiz-transcribe-whisper-local/directml"]
cuda = ["whisper-cpp", "notiz-transcribe-whisper-local/cuda"]
hipblas = ["whisper-cpp", "notiz-transcribe-whisper-local/hipblas"]
openblas = ["whisper-cpp", "notiz-transcribe-whisper-local/openblas"]
metal = ["whisper-cpp", "notiz-transcribe-whisper-local/metal"]
vulkan = ["whisper-cpp", "notiz-transcribe-whisper-local/vulkan"]
openmp = ["whisper-cpp", "notiz-transcribe-whisper-local/openmp"]

[dependencies]
notiz-transcribe-whisper-local = { workspace = true, optional = true }

axum = { workspace = true }
tokio = { workspace = true }
tower-http = { workspace = true, features = ["cors"] }
tracing = { workspace = true }

serde = { workspace = true }
specta = { workspace = true, optional = true }
tauri-specta = { workspace = true, optional = true, features = ["derive"] }
