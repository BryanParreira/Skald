[package]
name = "resampler"
version = "0.0.1"
edition = "2024"

[features]
default = ["async-source"]
async-source = [
  "dep:dasp",
  "dep:futures-util",
  "dep:skald-audio-interface",
  "dep:pin-project",
]

[dependencies]
audioadapter-buffers = "2.0"
dasp = { workspace = true, optional = true }
futures-util = { workspace = true, optional = true }
pin-project = { workspace = true, optional = true }
rubato = "1.0.1"
skald-audio-interface = { workspace = true, optional = true }
thiserror = { workspace = true }

[dev-dependencies]
hound = { workspace = true }
rodio = { workspace = true, features = ["symphonia-all"] }
skald-data = { workspace = true }
tokio = { workspace = true, features = ["full"] }

[[bench]]
name = "polynomial_degree"
harness = false
